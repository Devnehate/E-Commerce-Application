/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{
            hostname: "e-commerce-app1.oneentry.cloud",
        }]
    }
};

export default nextConfig;